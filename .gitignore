# Compiler files
cache/
out/

# Commonly ignored directories
.logs/
.ignore/

# Deployment artifacts — keep ALL broadcast logs private
# (they expose deployer address, tx calldata, timings, and deployed addresses).
/broadcast/

# Docs
docs/

# Dotenv file
.env
.env.*
!.env.example

# Private deployment material — never publish how/where we deploy.
DEPLOYMENT.md
deploy/
*.deploy.json
